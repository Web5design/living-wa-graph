%nav.navbar.navbar-default{:role => "navigation"}
  .navbar-header
    = link_to "The Maptastics", root_path, :class => 'navbar-brand'
  %ul.nav.navbar-nav
    - @organizations.each do |org|
      %li
        = link_to org.name, organization_path(org)
